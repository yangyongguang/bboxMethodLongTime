#include <iostream>
#include <fstream>
#include "param.h"
#include <vector>
#include <array>
#include <sstream>

using std::string;
using std::vector;
using std::stringstream;

int main()
{
	fprintf(stderr, "%d, %d, %d, %d\n", 0 ^ 0, 1 ^ 0, 1 ^ 1, 0 ^1);
	fprintf(stderr, "test2.cpp done\n"); 
	return 0;
}